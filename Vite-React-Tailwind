1.  create project folder
2.  go to terminal
3.  type in:
    `npm create vite@latest`
4. name your project
5. choose react
6. coose javascript or typescript
7. cd to the folder
8. type in:
    `npm i`
    `npm run dev`
9. on src create forlder and name it `components`
10. start creating components
11. type in:
    `rafce`
12. icons (optional) type in:
    `npm i react-icons --save`


TAILWIND
1. at terminal
    `npm install -D tailwindcss postcss autoprefixer`    
    `npx tailwindcss init -p`
2. at taiwlind.config.js
    /** @type {import('tailwindcss').Config} */
export default {
  `content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
      ],
      theme: {
    extend: {},
      },
      plugins: [],
    }`
3. at index.css
    `@tailwind base;
    @tailwind components;
    @tailwind utilities;`
